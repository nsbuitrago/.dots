# 2024-02-28

- Dropped steam and other games
- use nvim submodule for easier updating
- add rebuild.sh script for easier rebuilding

# 2024-02-18

- Add steam and switch kitty for wezterm
#
# 2024-02-12

Fix GitLab CI for testing nixos config builds

# 2024-02-11

- Fix cuda support

# 2024-02-10

- Neovim/zsh configuration embeded in home-manager config
- Add garbage collection of old nixos generations
- Add gitlab ci for testing new nixos config builds
- Use variables for username and hostname

# 2024-02-07

- Setup flakes + home-manager, first home-manager config for git
- Include kitty package

# 2024-02-06

- Setup vanilla nixos config with GNOME
- Add additional packages (nvim, firefox, python, cargo, tailscale, ...)
- Setup nvidia drivers and cuda support (cuda support not tested)
- Use zsh as default shell for all users
